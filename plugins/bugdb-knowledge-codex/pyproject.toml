[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "bugdb-knowledge-codex"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
bugdb-codex = "bugdb.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["bugdb"]

[tool.pytest.ini_options]
testpaths = ["tests"]
